#include <stdio.h>
int main(){
    // int side;
    // printf("Enter the side of square: ");
    // scanf("%d", &side);
    // printf("The area of the square is=%d" , side*side);

    float radius;
    printf("Enter the radius of the circle: ");
    scanf("%f", &radius);
    printf("The are of the circle is %f", 3.14*radius*radius);
    return 0 ;
}